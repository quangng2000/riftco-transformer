# Paths are resolved from the project directory.
corpus = data/pretraining/tiny_corpus.txt
results = results/tiny

# Tiny enough for a transparent CPU implementation.
seed = 42
context_size = 16
batch_size = 4
d_model = 32
n_heads = 4
n_layers = 2
d_ff = 64

# Training and sampling.
training_steps = 500
sample_every = 100
sample_length = 120

# Adam hyperparameters.
learning_rate = 0.001
adam_beta1 = 0.9
adam_beta2 = 0.999
adam_epsilon = 0.00000001
gradient_clip = 1.0
